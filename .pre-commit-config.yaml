---
default_stages: [pre-commit]

repos:
  - repo: https://github.com/ansible/ansible-lint
    rev: v24.12.0
    hooks:
      - id: ansible-lint
        files: ^ansible/.*\.(yml)$
        additional_dependencies:
          - ansible-navigator