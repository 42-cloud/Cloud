---
- name: Converge Host Bootstrap
  hosts: all
  gather_facts: true
  vars:
      bootstrap_delegate_host: "{{ inventory_hostname }}"
  tasks:
    - name: Include bootstrap role
      ansible.builtin.include_role:
        name: bootstrap
