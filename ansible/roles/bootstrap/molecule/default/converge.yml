---
- name: Converge Host Bootstrap
  hosts: all
  gather_facts: true
  roles:
    - role: bootstrap

  tasks:
    - name: "Include bootstrap role"
      ansible.builtin.include_role:
        name: "bootstrap"
